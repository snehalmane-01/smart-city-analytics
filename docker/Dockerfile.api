FROM python:3.11-slim
WORKDIR /app
COPY pyproject.toml README.md ./
COPY src ./src
RUN pip install --no-cache-dir -e . fastapi uvicorn
EXPOSE 8000
CMD ["uvicorn", "smart_city_analytics.serving:create_app", "--factory", "--host", "0.0.0.0", "--port", "8000"]
